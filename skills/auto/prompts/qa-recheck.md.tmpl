You must use Skill('ship:qa'). Skip preamble and auth gate.

Recheck after fixes: re-test previously failing criteria + check for regressions.
Skip exploratory testing (already done in first pass).

spec: {{TASK_DIR}}/plan/spec.md
task_dir: {{TASK_DIR}}
base_branch: {{BASE_BRANCH}}
Write reports to: {{TASK_DIR}}/qa/
Mode: /ship:auto recheck — no user questions.
