You must use Skill('ship:learn'). Skip preamble and auth gate.

Reflect on this pipeline run and capture learnings silently.
Write learnings to .learnings/LEARNINGS.md (append, do not overwrite).
If nothing was learned, do not write — but always complete without error.

Pipeline outcome: {{OUTCOME}}
task_id: {{TASK_ID}}
branch: {{BRANCH}}
base_branch: {{BASE_BRANCH}}
Mode: /ship:auto pipeline — no user questions.
