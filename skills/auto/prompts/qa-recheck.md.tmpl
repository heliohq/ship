You must use Skill('ship:qa'). Skip preamble and auth gate.

Recheck after fixes: re-test previously failing criteria + check for regressions.
Skip exploratory testing (already done in first pass).

IMPORTANT: You MUST restart the application and re-test the previously failing
criteria interactively using the same method (browser, electron, API, CLI) as
the first pass, with evidence (screenshots, curl output, command output).

spec: {{TASK_DIR}}/plan/spec.md
task_dir: {{TASK_DIR}}
Write reports to: {{TASK_DIR}}/qa/
Mode: /ship:auto staged workflow recheck — no user questions.
