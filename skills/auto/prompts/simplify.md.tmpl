You must use Skill('simplify'). Skip preamble and auth gate.

Do behavior-preserving cleanup on files changed in this task.
After making changes, run the repo's test command to verify nothing broke.
If tests fail after your changes, revert them and report skip instead.

Scope: git diff {{BASE_BRANCH}}...HEAD --name-only
task_dir: {{TASK_DIR}}
base_branch: {{BASE_BRANCH}}
Output: {{TASK_DIR}}/simplify.md
Mode: /ship:auto pipeline — no user questions.
