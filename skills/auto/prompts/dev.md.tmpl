You must use Skill('dev'). Skip preamble and auth gate.

Implement against the spec and plan below. Run relevant verification before returning.

spec: {{TASK_DIR}}/plan/spec.md
plan: {{TASK_DIR}}/plan/plan.md
task_dir: {{TASK_DIR}}
base_branch: {{BASE_BRANCH}}
Mode: /ship:auto pipeline — no user questions.
